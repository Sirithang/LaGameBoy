#include "graphic.h"

using namespace graphic;

void graphic::drawTile(CPU& cpu, u16 x, u16 y, u8 tilenum, u8* screen, u16 screenWidth)
{
	for (u16 px = 0; px < 8; ++px)
	{
		for (u16 py = 0; py < 8; ++py)
		{
			//screen[]
		}
	}
}